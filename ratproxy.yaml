listen: 0.0.0.0:1414
# Uncomment the following line if you want to use a self-signed TLS cert for the main listener.
# useTLS: true
targets:
  - name: service1
    target: http://localhost:10000
    path: /service1/
    type: lambda
  - name: service2
    target: http://localhost:1313
    path: /
